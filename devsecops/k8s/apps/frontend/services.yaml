apiVersion: v1
kind: Service
metadata:
  name: frontend-stable
  namespace: app-frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-canary
  namespace: app-frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 8000
      targetPort: 8000
