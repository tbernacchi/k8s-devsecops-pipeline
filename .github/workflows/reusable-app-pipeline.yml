name: Reusable Secure App Pipeline

on:
  workflow_call:
    inputs:
      app_name:
        required: true
        type: string
      app_path:
        required: true
        type: string
      dockerfile_path:
        required: true
        type: string
      codeql_language:
        required: true
        type: string
      dependency_file:
        required: true
        type: string
      deploy_name:
        required: true
        type: string
      container_name:
        required: true
        type: string
      route_prefix:
        required: true
        type: string
    secrets:
      SONAR_TOKEN:
        required: false
      SONAR_HOST_URL:
        required: false
      SNYK_TOKEN:
        required: false
      DEV_KUBECONFIG_B64:
        required: false
      STAGING_KUBECONFIG_B64:
        required: false
      PROD_KUBECONFIG_B64:
        required: false
      STAGING_BASE_URL:
        required: false
      PROD_BASE_URL:
        required: false

permissions:
  contents: read
  security-events: write

jobs:
  stage1_secret_detection:
    name: "Stage 1 - Secret Detection"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: detect-secrets as pre-commit hook
        run: |
          pip install pre-commit detect-secrets
          cat <<'EOF' > .pre-commit-ci.yaml
          repos:
            - repo: https://github.com/Yelp/detect-secrets
              rev: v1.5.0
              hooks:
                - id: detect-secrets
          EOF
          pre-commit run --config .pre-commit-ci.yaml --all-files
      - name: Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: TruffleHog
        run: |
          docker run --rm -v "$PWD:/pwd" trufflesecurity/trufflehog:latest filesystem /pwd --fail --no-update

  stage2_sast:
    name: "Stage 2 - SAST"
    needs: stage1_secret_detection
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
    steps:
      - uses: actions/checkout@v4
      - uses: returntocorp/semgrep-action@v1
        with:
          config: auto
      - uses: github/codeql-action/init@v3
        with:
          languages: ${{ inputs.codeql_language }}
      - if: inputs.codeql_language == 'go'
        uses: github/codeql-action/autobuild@v3
      - uses: github/codeql-action/analyze@v3
      - name: SonarQube Scan
        if: env.SONAR_TOKEN != '' && env.SONAR_HOST_URL != ''
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ env.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ env.SONAR_HOST_URL }}
        with:
          args: >
            -Dsonar.projectKey=${{ github.repository }}-${{ inputs.app_name }}
            -Dsonar.sources=${{ inputs.app_path }}
      - name: SonarQube Quality Gate
        if: env.SONAR_TOKEN != '' && env.SONAR_HOST_URL != ''
        uses: SonarSource/sonarqube-quality-gate-action@v1.2.0
        with:
          pollingTimeoutSec: "300"
        env:
          SONAR_TOKEN: ${{ env.SONAR_TOKEN }}
      - name: SonarQube skipped
        if: env.SONAR_TOKEN == '' || env.SONAR_HOST_URL == ''
        run: echo "Skipping SonarQube because SONAR_TOKEN/SONAR_HOST_URL are not configured."

  stage3_sca:
    name: "Stage 3 - SCA"
    needs: stage2_sast
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trivy dependency scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: ${{ inputs.app_path }}
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: "1"
      - name: OWASP Dependency Check
        uses: dependency-check/Dependency-Check_Action@main
        with:
          project: ${{ inputs.app_name }}
          path: ${{ inputs.app_path }}
          format: HTML
          out: reports
          args: >
            --enableRetired
            --failOnCVSS 7
      - name: Snyk (Go)
        if: inputs.codeql_language == 'go'
        continue-on-error: true
        uses: snyk/actions/golang@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high --file=${{ inputs.dependency_file }}
      - name: Snyk (Python)
        if: inputs.codeql_language == 'python'
        continue-on-error: true
        uses: snyk/actions/python@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high --file=${{ inputs.dependency_file }}
      - name: Snyk skipped
        if: inputs.codeql_language != 'go' && inputs.codeql_language != 'python'
        run: echo "Skipping Snyk because no mapped action exists for language '${{ inputs.codeql_language }}'."

  stage4_container_iac_scanning:
    name: "Stage 4 - Container and IaC Scanning"
    needs: stage3_sca
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build image for scanning
        run: |
          docker build -f "${{ inputs.dockerfile_path }}" -t "local/${{ inputs.app_name }}:${{ github.sha }}" "${{ inputs.app_path }}"
      - name: Trivy image scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: local/${{ inputs.app_name }}:${{ github.sha }}
          severity: HIGH,CRITICAL
          exit-code: "1"
      - name: Grype image scan
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin
          grype "local/${{ inputs.app_name }}:${{ github.sha }}" --fail-on high
      - name: Anchore image scan
        uses: anchore/scan-action@v6
        with:
          image: local/${{ inputs.app_name }}:${{ github.sha }}
          fail-build: true
          severity-cutoff: high
      - name: Checkov IaC scan
        if: hashFiles('devops-system-design-challenge/infra/**') != ''
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: devops-system-design-challenge/infra
      - name: tfsec IaC scan
        if: hashFiles('devops-system-design-challenge/infra/**/*.tf') != ''
        uses: aquasecurity/tfsec-action@v1.0.3
        with:
          working_directory: devops-system-design-challenge/infra
      - name: Conftest IaC scan
        if: hashFiles('devops-system-design-challenge/infra/**') != ''
        run: |
          wget -qO- https://github.com/open-policy-agent/conftest/releases/download/v0.54.0/conftest_0.54.0_Linux_x86_64.tar.gz | tar xz
          sudo mv conftest /usr/local/bin
          cat <<'EOF' > /tmp/infra.rego
          package main
          deny[msg] {
            input.kind == "Deployment"
            not input.spec.template.spec.securityContext.runAsNonRoot
            msg := "runAsNonRoot must be true"
          }
          EOF
          conftest test devops-system-design-challenge/infra -p /tmp
      - name: Kyverno policy-as-code validation
        if: hashFiles('devops-system-design-challenge/infra/helm/**/*.y*ml', 'devops-system-design-challenge/k8s/**/*.y*ml') != ''
        run: |
          KYVERNO_VERSION="v1.12.5"
          curl -sSfL "https://github.com/kyverno/kyverno/releases/download/${KYVERNO_VERSION}/kyverno-cli_${KYVERNO_VERSION#v}_linux_x86_64.tar.gz" | tar xz
          sudo mv kyverno /usr/local/bin/kyverno

          cat <<'EOF' > /tmp/kyverno-ci-policies.yaml
          apiVersion: kyverno.io/v1
          kind: ClusterPolicy
          metadata:
            name: require-pod-security-baseline
          spec:
            validationFailureAction: Enforce
            background: false
            rules:
              - name: require-run-as-non-root
                match:
                  any:
                    - resources:
                        kinds:
                          - Pod
                validate:
                  message: "runAsNonRoot must be set to true."
                  pattern:
                    spec:
                      securityContext:
                        runAsNonRoot: true
              - name: require-read-only-root-filesystem
                match:
                  any:
                    - resources:
                        kinds:
                          - Pod
                validate:
                  message: "Containers must set readOnlyRootFilesystem to true."
                  foreach:
                    - list: "request.object.spec.containers"
                      pattern:
                        securityContext:
                          readOnlyRootFilesystem: true
          EOF

          shopt -s globstar nullglob
          manifest_files=(devops-system-design-challenge/infra/helm/**/*.yaml devops-system-design-challenge/infra/helm/**/*.yml devops-system-design-challenge/k8s/**/*.yaml devops-system-design-challenge/k8s/**/*.yml)
          for file in "${manifest_files[@]}"; do
            kyverno apply /tmp/kyverno-ci-policies.yaml --resource "$file"
          done

  stage5_build_sign_attest:
    name: "Stage 5 - Build and Artifact Signing"
    needs: stage4_container_iac_scanning
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    outputs:
      image_ref: ${{ steps.meta.outputs.image_ref }}
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        run: |
          echo "image_ref=ghcr.io/${{ github.repository_owner }}/system-design-2026-${{ inputs.app_name }}" >> "$GITHUB_OUTPUT"
      - id: build
        uses: docker/build-push-action@v6
        with:
          context: ${{ inputs.app_path }}
          file: ${{ inputs.dockerfile_path }}
          push: true
          tags: ${{ steps.meta.outputs.image_ref }}:${{ github.sha }}
          provenance: true
          sbom: true
      - uses: sigstore/cosign-installer@v3
      - name: Cosign sign
        run: |
          cosign sign --yes "${{ steps.meta.outputs.image_ref }}@${{ steps.build.outputs.digest }}"
      - uses: actions/attest-build-provenance@v1
        with:
          subject-name: ${{ steps.meta.outputs.image_ref }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true

  deploy_dev:
    name: "Stage 6 - Dev Auto Deploy"
    needs: stage5_build_sign_attest
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: azure/setup-kubectl@v4
      - name: Deploy same signed artifact to dev
        env:
          KCFG: ${{ secrets.DEV_KUBECONFIG_B64 }}
          IMAGE: ${{ needs.stage5_build_sign_attest.outputs.image_ref }}@${{ needs.stage5_build_sign_attest.outputs.digest }}
        run: |
          test -n "$KCFG"
          echo "$KCFG" | base64 -d > kubeconfig
          export KUBECONFIG="$PWD/kubeconfig"
          kubectl -n app set image deployment/${{ inputs.deploy_name }} ${{ inputs.container_name }}="$IMAGE"
          kubectl -n app rollout status deployment/${{ inputs.deploy_name }} --timeout=180s

  deploy_staging:
    name: "Stage 7 - Staging Auto Deploy"
    needs: [deploy_dev, stage5_build_sign_attest]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: azure/setup-kubectl@v4
      - name: Deploy same signed artifact to staging
        env:
          KCFG: ${{ secrets.STAGING_KUBECONFIG_B64 }}
          IMAGE: ${{ needs.stage5_build_sign_attest.outputs.image_ref }}@${{ needs.stage5_build_sign_attest.outputs.digest }}
        run: |
          test -n "$KCFG"
          echo "$KCFG" | base64 -d > kubeconfig
          export KUBECONFIG="$PWD/kubeconfig"
          kubectl -n app set image deployment/${{ inputs.deploy_name }} ${{ inputs.container_name }}="$IMAGE"
          kubectl -n app rollout status deployment/${{ inputs.deploy_name }} --timeout=180s

  stage6_dast:
    name: "Stage 8 - DAST"
    needs: deploy_staging
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: zaproxy/action-baseline@v0.14.0
        with:
          target: ${{ secrets.STAGING_BASE_URL }}${{ inputs.route_prefix }}
          fail_action: true

  staging_acceptance:
    name: "Stage 9 - Staging Acceptance"
    needs: stage6_dast
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Acceptance smoke test
        run: |
          test -n "${{ secrets.STAGING_BASE_URL }}"
          curl -fsS "${{ secrets.STAGING_BASE_URL }}${{ inputs.route_prefix }}/healthz"

  policy_gate:
    name: "Stage 10 - Policy Gate"
    needs: staging_acceptance
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: OPA gate in CI
        run: |
          wget -qO- https://github.com/open-policy-agent/conftest/releases/download/v0.54.0/conftest_0.54.0_Linux_x86_64.tar.gz | tar xz
          sudo mv conftest /usr/local/bin
          cat <<'EOF' > policy-input.json
          {"signed_artifact": true, "dast_passed": true, "acceptance_passed": true}
          EOF
          cat <<'EOF' > policy.rego
          package main
          deny[msg] { not input.signed_artifact; msg := "signed artifact required" }
          deny[msg] { not input.dast_passed; msg := "dast required" }
          deny[msg] { not input.acceptance_passed; msg := "acceptance required" }
          EOF
          conftest test policy-input.json -p .

  deploy_prod:
    name: "Stage 11 - Prod Approval and Progressive Deploy"
    needs: [policy_gate, stage5_build_sign_attest]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: azure/setup-kubectl@v4
      - name: Progressive rollout with auto rollback
        env:
          KCFG: ${{ secrets.PROD_KUBECONFIG_B64 }}
          PROD_BASE_URL: ${{ secrets.PROD_BASE_URL }}
          IMAGE: ${{ needs.stage5_build_sign_attest.outputs.image_ref }}@${{ needs.stage5_build_sign_attest.outputs.digest }}
        run: |
          test -n "$KCFG"
          echo "$KCFG" | base64 -d > kubeconfig
          export KUBECONFIG="$PWD/kubeconfig"
          kubectl -n app set image deployment/${{ inputs.deploy_name }} ${{ inputs.container_name }}="$IMAGE"
          if ! kubectl -n app rollout status deployment/${{ inputs.deploy_name }} --timeout=240s; then
            kubectl -n app rollout undo deployment/${{ inputs.deploy_name }}
            exit 1
          fi
          if [ -n "$PROD_BASE_URL" ]; then
            if ! curl -fsS "${PROD_BASE_URL}${{ inputs.route_prefix }}/healthz"; then
              kubectl -n app rollout undo deployment/${{ inputs.deploy_name }}
              exit 1
            fi
          fi
